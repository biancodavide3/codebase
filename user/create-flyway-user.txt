# LOGIN
psql

# CREATE DB (if it doesn't exist)
CREATE DATABASE userdb;

# CONNECT TO DB
\c userdb

# CREATE FLYWAY USER (with fine-grained permissions on the schema)
CREATE USER flyway PASSWORD 'password';
GRANT CREATE, USAGE ON SCHEMA public TO flyway;
